import os
from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Axole- is running!"

# This is where your bot logic will go
# Example: Discord or Telegram bot code here
def start_bot():
    print("Axole- Bot Started...")
    # TODO: Add your bot token logic

if __name__ == "__main__":
    start_bot()
    # Render needs a web server on port 10000
    port = int(os.environ.get("PORT", 10000))
    app.run(host="0.0.0.0", port=port)